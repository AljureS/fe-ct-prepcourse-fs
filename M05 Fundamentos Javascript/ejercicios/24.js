let argX = 20
let argY = 5

function divide(x, y) {
  // La función recibe dos argumentos "x" e "y" que son números.
  // Retorna el resultado de su división.
  // Tu código:
  return x / y
}

console.log(divide(argX,argY));
module.exports = divide;