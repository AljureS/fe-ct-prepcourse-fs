let argX = 35
let argY = 5

function suma(x, y) {
  // La función recibe dos argumentos "x" e "y" que son números.
  // Retorna el resultado de su suma.
  // Tu código:
  return x + y
}

console.log(suma(argX,argY));
module.exports = suma;