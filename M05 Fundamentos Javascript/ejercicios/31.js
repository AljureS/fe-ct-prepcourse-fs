let c1 = 10
let c2 = 0

function areaDelTriangulo(base, altura) {
  // Calcula el área de un triángulo y retorna el resultado.
  // Tu código:
  if ((base >= 0) && (altura >= 0)){
    return ((base * altura)/2)
  }
}
console.log(areaDelTriangulo(c1,c2));

module.exports = areaDelTriangulo;
